DROP TABLE IF EXISTS categories CASCADE; --Должна идти после users

CREATE TABLE IF NOT EXISTS categories (
    id  BIGINT  GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(50) UNIQUE NOT NULL
);